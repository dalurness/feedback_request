import {createTheme} from '@material-ui/core/styles'

let Theme = createTheme({
    palette: {
        primary: {
            main: '#42860e',
        },
        secondary: {
            main: '#005c7d',
        },
        background: {
            default: '#fff',
        },
    }
})

export default Theme